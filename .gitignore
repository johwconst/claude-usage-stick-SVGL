# ───────── Segredos — NUNCA commitar ─────────
# .env: token OAuth do Claude
# .mcp.json: contém chave da OpenAI no ambiente do autor
.env
.env.*
.mcp.json
secrets.h
*.token

# ───────── Config local de ferramentas ─────────
# .claude/: config/skills locais do Claude Code (não faz parte do projeto)
.claude/
.vscode/
.idea/
CLAUDE.md
# docs locais que nao vao para o repo publico
LOGO-MONITOR-DE-USO.md
# material de divulgacao (carrosseis, legendas) — fica local
marketing/
# geradores de imagem (mockups, banners, social, renders, instagram) — ficam locais
tools-local/
# ───────── Build / artefatos ─────────
firmware/*/build/
.pio/
*.bin
*.elf
*.hex

# ───────── SO ─────────
.DS_Store
Thumbs.db
__pycache__/
